training:
  experiment_name: "voicellm_kaggle_t4_run1"
  batch_size: 4
  grad_accum_steps: 8
  learning_rate: 0.0002
  min_learning_rate: 0.00001
  warmup_steps: 1000
  max_steps: 40000
  weight_decay: 0.01
  max_grad_norm: 1.0
  mixed_precision: "fp16"
  seed: 42
  log_every_steps: 20
  eval_every_steps: 500
  save_every_steps: 1000
  output_dir: "/kaggle/working/outputs"
  wandb_project: "voicellm-kaggle"
